function eatDot() {
  console.log('MUNCH!');
}
var eatDot = function() {
  console.log('MUNCH!');
}

var hero = 'Pac-Man'
console.log(hero.toUpperCase);
[function: toUpperCase]

hero.toUpperCase();
console.log(hero.length);

var highScores = [];
function addHighScore(highScoreList, playerInitals, score) {
  var record = {player: playerInitals, score: score };
  highScoreList.push(record);
}
addHighScore(highScores, 'SDL', 2000);

{ player: 'SDL', score: 2000 } ]
addHighScore(highScores, 'JSL', 10000);

[ { player: 'SDL', score: 2000 },
  { player: 'JSL', score: 10000 } ]
